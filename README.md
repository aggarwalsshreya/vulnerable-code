# vulnerable-code
Included vulnerability types: SQL injection, command injection, code injection, path traversal, unsafe deserialization, hardcoded secrets, weak crypto, insecure randomness, SSRF, XSS, Flask debug mode, XXE, insecure TLS, open redirect, CORS misconfiguration, log injection, insecure temp file, Zip Slip, missing request timeout, assert-based auth, JWT signature bypass, mass assignment, integer overflow/truncation simulation, out-of-bounds/index errors, null/None dereference, resource leak, race condition, LDAP injection, template injection, raw SQL, insecure cookies, bad file permissions, CSV formula injection.
